#include <stdio.h>

/**
* main - A program that print a line followed by new line using the function printf
*
* Return: Always 0 (Success)
*/
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
